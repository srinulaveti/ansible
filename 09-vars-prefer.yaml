- name: variable preference
  hosts: localhost
  # vars:
  #   name: "my name is srinu"
  vars_files:
  - vars.yaml
  # vars_prompt:
  # - name: stream
  #   prompt: Please enter the branch
  #   private: false
  tasks:
  - name: check preference
    # vars:
    #   name: "Morning from TASK variable"
    ansible.builtin.debug:
      msg: "Hello,{{name}}"

# 1. arguments
# 2. Task level
# 3. variable files
# 4. Prompt
# 5. Play level
# 6. inventory
# 7. role level